apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: observability-lab
  labels:
    app: orders-api
spec:
  type: NodePort
  selector:
    app: orders-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
